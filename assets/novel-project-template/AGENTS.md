# 小说项目工作约定

- `background/`、`bible/`、`plot/`、`outlines/`、`chapters/` 和 `ledger/author-decisions.md` 构成项目正史工作区。
- 未经作者确认的候选方案只能留在决策登记表，不能进入正史文件。
- 只有 `chapters/` 中符合章节格式的正文是可发布文本。
- 修改已锁定设定时，记录新的 `D-编号` 并做回归检查。
- 中文正文和作者沟通使用项目指定语言；内部字段可保持稳定的英文键名。
