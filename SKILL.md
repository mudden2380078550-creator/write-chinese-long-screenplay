---
name: write-chinese-long-screenplay
description: 规划、创作、续写和审查中文长篇小说与中文电影/剧集长剧本。根据用户选择的小说或剧本模式维护背景、人物、结构、正文、连续性和作者决策；AI 可提出有限候选方案，但未经作者确认不得写入正史。
---

# 中文长篇写作

本 Skill 现在支持两种交付模式：

- **小说模式**：中文长篇小说、网文、连载、章节和出版正文；
- **剧本模式**：中文电影、剧集、短剧和动画剧本，保留原有场次、对白和可拍摄性流程。

两种模式共用作者决策门禁、背景设定、人物设定、连续性台账和回归审校，但正文结构不同。读取 [决策门禁](references/decision-gates.md)；小说请求再读取 [小说模式](references/novel-mode.md)。剧本请求沿用现有剧本参考资料和校验脚本。

## 模式路由

- 用户说“小说、长篇、章节、卷纲、连载、网文、正文、EPUB”时，选择小说模式。
- 用户说“剧本、电影、剧集、分集、场次、对白、拍摄”时，选择剧本模式。
- 两者都出现且交付物不明确时，先询问当前要交付小说正文还是剧本文件。
- 不要把小说章节写成剧本场次，也不要把剧本格式强行套在小说正文上。

## 作者决策门禁（两种模式都必须执行）

AI 负责发散、比较、检查和执行；作者负责选择、取舍和最终授权。

1. 需要设计关键设定时，最多提出三个互斥方案。
2. 每个方案说明核心内容、优点、代价、影响范围和推荐理由。
3. 输出候选后必须停在“请作者选择”，不能把推荐方案默认写入设定包。
4. 作者明确选择后，记录唯一 `D-编号` 到 `ledger/author-decisions.md`，状态设为 `CANON`。
5. 只有 `CANON` 内容能进入背景、人物、总纲、章节、场次或台账正典；候选方案标记为 `DEPRECATED`。
6. 已锁定设定要变更时，创建新的变更决策，列出受影响文件并完成回归检查。
7. 正史文件禁止出现“方案A/方案B”“可以是……也可以是……”和未确认的分支表达。

## 正典优先级

1. 作者当前明确指令；
2. `CANON` 作者决策和已确认正文；
3. 背景、人物、世界规则和故事台账；
4. 总纲、卷纲、分集/章节/场次大纲；
5. AI 的推荐和诊断。

低层级资料不得静默覆盖高层级事实。发现冲突时，报告冲突、影响范围和需要作者确认的决策，不自行折中。

## 共用创作输入

### 背景设定

只记录会改变人物选择、代价、风险、时间、空间或读者/观众判断的内容：时代、地点、制度、历史余波、世界规则、资源、限制、秘密、物件和不可改变的事实。

### 人物设定

每个主要人物至少明确：外在欲望、内在需要、错误信念、保护策略、能力、资源、限制、秘密、知识边界、关系交换、压力下行为、语言习惯和最终选择。

背景和人物不是百科资料。每条内容都要回答：它迫使谁做什么选择？不遵守有什么代价？何时会以可见后果进入作品？

## 共同故事内核

```text
主题/承诺 → 主角欲望 → 激励性扰动 → 递进复杂化
→ 不可回头点 → 危机选择 → 高潮行动 → 结局价值与余波
```

每个章节或场次都必须形成：

```text
继承状态 → 当前压力 → 人物策略 → 对手/环境反制
→ 预期与实际结果落差 → 价值/关系变化 → 下一步压力
```

## 小说模式

阅读 [references/novel-mode.md](references/novel-mode.md)。小说正文写叙述、视角、内心、段落节奏和可读性；使用 `plot/`、`outlines/`、`chapters/` 和 `ledger/`。章节写入前必须完成决策门禁和连续性检查，章节完成后回写台账。

## 剧本模式

剧本模式保留原有工作方式：

- `background/story-background.md` 和 `bible/characters/*.md` 是用户主要输入；
- `outline/`、`screenplay/scenes/`、`ledger/` 是内部生成与维护资料；
- 每场先定义来源、视点/目标、冲突/策略、预期结果、实际结果、结果落差、价值变化和下场压力；
- 正文写可见、可听、可表演的动作和具有对象与策略的对白；
- 使用 `scripts/write_scene.py`、`scripts/validate_project.py`、`scripts/self_review.py` 和 `scripts/compile_screenplay.py` 完成安全写入、严格校验和导出；
- 中文自然度审查继续使用 `references/natural-chinese.md`。

## 工程流程

1. 读取 `AGENTS.md`、`project.md`、作者决策登记、背景和人物输入。
2. 识别模式，建立有限上下文包。
3. 检查关键设定是否已经 `CANON`；未确认时先走决策门禁。
4. 生成当前模式的结构计划和交付物契约。
5. 执行小说章节或剧本场次写作。
6. 运行对应校验和中文自然度/读者模型审查。
7. 只有作者接受后，才把新事实写回正典和台账。

## 完成门禁

不得把以下状态报告为“完成”：

- 仍有未确认的关键方案；
- 正史文件残留候选分支或协作元话语；
- 人物知识、时间、物件、关系或规则成本不连续；
- 小说章节没有继承状态、不可逆变化或出口问题；
- 剧本场次没有可拍摄动作、策略性对白或价值变化；
- 校验脚本存在未处理错误；
- 新正典尚未经过作者确认。

